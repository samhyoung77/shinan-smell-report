# QA Report — 광역 지도 + 확대/축소 (2026-07-05)

로컬 서버(8899) + Chrome 실측. 홈(등록) 지도.

## Summary
- BLOCKER: 0 / MAJOR: 0 / MINOR: 0 (초기 auto-zoom 이슈는 QA 중 수정 완료)
- 결과: **PASS**

## 검증
| 항목 | 결과 |
|------|------|
| 광역 개략도(개울이 좌·하·우 감쌈) | ✅ U자 파란 리본, 흐름 화살표, "개울" 라벨 |
| 잡텍스트 제거 | ✅ POI/도로명 전부 없음(문현로 라벨 1개만) |
| 12개 동 배치(신규 좌표) | ✅ 새 이미지와 일치, id/name/complex 불변 |
| 기본 전체 조망 | ✅ (초기 auto-zoom→selected 를 scale 1 전경으로 수정) |
| ＋/－ 줌 | ✅ 커서/중앙 기준, 클램프로 빈 여백 없음 |
| 드래그 팬 | ✅ 이동, 선택 안 바뀜(탭/드래그 6px 임계) |
| 탭 선택 | ✅ 확대 상태서 204동 탭→selected/라벨/localStorage b204 |
| 리셋(⟳) | ✅ 전체 조망 복귀 |
| 라이트/다크 | ✅ 물색 토큰 자동 대응(라이트 #4aa3e0 / 다크 #2b6fa8) |
| 회귀(선택·뱃지·강도색·통계 계약) | ✅ heatmap/exposure/trend/stats 불변 |
| node --check | ✅ buildings.js, map-overlay.js PASS |

## QA 중 수정
- 초기 로드가 저장된 동으로 2.2x 자동 확대되어 "개울이 안 보이는" 문제 재현 → `renderMap` 초기 로직을 **항상 scale 1 전체 조망**으로 변경(선택 동은 하이라이트로만). 사용자 요구("전체적으로 보여지고, 확대는 직접")와 일치.

## 관찰(블로커 아님)
- 줌 버튼(＋/－/⟳)이 우측에서 개울 우측 팔과 살짝 겹침 — 플로팅 컨트롤 관행상 허용. 필요 시 위치/크기 조정 가능.
- 건물 footprint 블록은 원 뒤에서 옅음(확대 시 더 보임). 위치·라벨은 원이 담당.
- 오버레이 라벨은 줌 시 함께 스케일(고정 크기 아님) — 과대/과소 없이 가독.

STATUS: DONE
